# Exemplo de arquivo .env para configuração de variáveis de ambiente

# String de conexão com o banco de dados
ConnectionStrings__Default=CONNECTION_STRING

# Configurações do JWT 
JWT_KEY=CHAVE_JWT
JWT_ISSUER=USUARIO_JWT
JWT_AUDIENCE=CLIENTE_JWT